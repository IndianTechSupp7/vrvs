import { View } from "react-native";
import { Text } from "../CustomText";

const Options = () => {
  return (
    <View className="flex-1 justify-center items-center border">
      <Text className="text-text-light font-poppins-bold">Options</Text>
    </View>
  );
};

export default Options;
